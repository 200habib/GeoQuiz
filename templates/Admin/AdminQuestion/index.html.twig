{% extends 'base.html.twig' %}

{% block title %}📋 Liste des Questions{% endblock %}

{% block body %}
<div class="flex justify-center items-start bg-gray-100 p-6">
    <div class="w-full max-w-6xl">

        <!-- Titolo -->
        <h1 class="text-4xl font-bold text-blue-600 text-center mb-6">📋 Liste des Questions</h1>

        <!-- Container per le domande (grid a 3 colonne) -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for question in questions %}
                <div class="bg-white p-6 shadow-lg rounded-xl border border-gray-300 hover:shadow-xl transition">
                    <div class="flex justify-between items-center mb-4">
                        <h2 class="text-xl font-semibold text-blue-600">❓ Question ID: {{ question.id }}</h2>
                    </div>
                    <p class="text-lg mb-4">📄 Question: {{ question.questionText }}</p>

                    <div class="flex space-x-4">
                        <a href="{{ path('app_question_show', {'id': question.id}) }}" class="text-green-600 font-bold hover:underline">👀 Voir</a>
                        <a href="{{ path('app_question_edit', {'id': question.id}) }}" class="text-yellow-600 font-bold hover:underline">✏️ Modifier</a>
                    </div>
                </div>
            {% else %}
                <div class="bg-white p-6 shadow-lg rounded-xl border border-gray-300 text-center col-span-3">
                    <p class="text-gray-500">🚫 Aucun enregistrement trouvé</p>
                </div>
            {% endfor %}
        </div>

        <!-- Bottone per creare una nuova domanda -->
        <div class="text-center mt-6">
            <a href="{{ path('app_question_new') }}" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg shadow-md transition">
                ➕ Créer une nouvelle question
            </a>
        </div>

    </div>
</div>
{% endblock %}
