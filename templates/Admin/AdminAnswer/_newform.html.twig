

<div class="container mx-auto p-6 max-w-lg bg-white shadow-lg rounded-lg">
    <h1 class="text-2xl font-bold text-center text-blue-600">📝 Remplissez le formulaire</h1>

    {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}

    <div class="space-y-4">
        {% for answerForm in form.answers %}
            <div class="border p-4 rounded-md shadow">
                <div>
                    <label class="block text-gray-700 font-semibold">✍️ Réponse :</label>
                    {{ form_widget(answerForm.answer_text, {'attr': {'class': 'w-full p-2 border border-gray-300 rounded-md'}}) }}
                </div>
                <div class="flex items-center mt-6">
                    <label class="w-xl text-gray-700 font-semibold">✅ Est-ce que cette réponse doit être la bonne réponse ?</label>
                    {{ form_widget(answerForm.is_correct, {'attr': {'class': 'w-full p-2 border border-gray-300 rounded-md'}}) }}
                </div>


                {# bla bla bla #}

                <div class="mt-6">
                    <label class="block text-gray-700 font-semibold">📝 Seleziona un quiz:</label>
                    <select id="quizFilter" class="w-full p-2 border border-gray-300 rounded-md">
                        <option value="">Tutti i quiz</option>
                        {% for choice in quiz %}
                            <option value="{{ choice.id }}">{{ choice.title }}</option>
                        {% endfor %}
                    </select>
                </div>
                
                {# bla bla bla #}
                
                <div class="mt-6">
                    <label class="block text-gray-700 font-semibold">❓ Question associée :</label>
                    {{ form_widget(answerForm.question, {'attr': {'class': 'w-full p-2 border border-gray-300 rounded-md'}}) }}
                
                </div>
                
                
            </div>
        {% endfor %}
    </div>

    <button class="mt-6 w-full bg-green-500 text-white p-2 rounded-md hover:bg-green-600 transition">
        💾 {{ button_label|default('Enregistrer') }}
    </button>

    {{ form_end(form) }}
</div>

<pre>{{ dump(quiz) }}</pre>

