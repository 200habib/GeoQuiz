{% extends 'base.html.twig' %}

{% block title %}Category Index{% endblock %}

{% block body %}
<section class="container mx-auto my-12 p-4">
    <h2 class="text-4xl font-extrabold text-center mb-10 text-gray-900">🧠 Choisissez votre Quiz</h2>
    <p class="text-center text-gray-600 max-w-2xl mx-auto mb-12">
        Testez vos connaissances en géographie avec nos différents quiz. Sélectionnez une catégorie et commencez à jouer immédiatement ! 🎯
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for category in categories %}
        <div class="bg-white p-6 rounded-lg shadow-lg hover:shadow-2xl transition duration-300 transform hover:scale-105">
            <h3 class="text-2xl font-semibold mb-3 text-gray-800">{{ category.name }}</h3>
            <p class="text-gray-600 mb-4">{{ category.description }}</p>
            
            <div class="flex justify-between items-center">
                <a href="{{ path('app_category_show', {'id': category.id}) }}" 
                   class="text-blue-500 hover:text-blue-700 transition duration-200">Show</a>
                <a href="{{ path('app_category_edit', {'id': category.id}) }}" 
                   class="text-green-500 hover:text-green-700 transition duration-200">Edit</a>
            </div>
        </div>
        {% else %}
        <div class="text-center text-gray-500 col-span-3">No categories found</div>
        {% endfor %}
    </div>

    <div class="text-center mt-12">
        <a href="{{ path('app_category_new') }}" 
           class="bg-blue-600 text-white py-2 px-6 rounded-lg hover:bg-blue-700 transition duration-300">Create New Category</a>
    </div>
</section>
{% endblock %}
