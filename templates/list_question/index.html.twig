{% extends 'base.html.twig' %}

{% block title %}📖 {{ quiz.title }}{% endblock %}

{% block body %}
    <div class="container mx-auto p-6 max-w-2xl bg-white shadow-lg rounded-lg">
        <h1 class="text-2xl font-bold text-center text-blue-600">📚 {{ quiz.title }}</h1>
        
        {% for question in quiz.questions %}
            <div class="question bg-gray-100 p-4 mt-4 rounded-lg">
                <p class="text-lg font-semibold">❓ {{ question.questionText }}</p>
                <ul class="mt-2 grid grid-cols-2 gap-2">
                    {% for answer in question.answers %}
                        <li class="p-2 bg-blue-100 rounded-md hover:bg-blue-200 transition text-center">✅ {{ answer.answerText }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% else %}
            <p class="text-center text-gray-500 mt-4">⚠️ Aucune question disponible pour ce quiz.</p>
        {% endfor %}
    </div>
{% endblock %}