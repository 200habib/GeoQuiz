<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="container mx-auto px-4">
        <div class="max-w-lg mx-auto bg-white p-8 rounded-2xl shadow-xl">
<h1 class="text-center text-4xl font-bold text-blue-600">
    {{ button_label }}
</h1>
            {{ form_start(form, { 'attr': {'class': 'space-y-6'} }) }}

            <div>
                <label class="block text-gray-800 font-medium mb-2">
                    {{ form_label(form.name) }}
                </label>
                {{ form_widget(form.name, {
                    'attr': {
                        'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition',
                        'placeholder': 'Enter category name'
                    }
                }) }}
            </div>

            <div>
                <label class="block text-gray-800 font-medium mb-2">
                    {{ form_label(form.description) }}
                </label>
                {{ form_widget(form.description, {
                    'attr': {
                        'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition',
                        'placeholder': 'Enter category description'
                    }
                }) }}
            </div>

            <button class="w-full bg-blue-600 text-white font-semibold py-3 rounded-xl hover:bg-blue-700 focus:ring-2 focus:ring-blue-400 transition transform hover:scale-100 active:scale-95">
                {{ button_label|default('Save') }}
            </button>

            {{ form_end(form) }}
        </div>
    </div>
</div>
